Any dependencies that one of more classes relies on go into common, even if one of those classes is itself a dependency. The difference between a class and a dependency is that a class represents high level functions (everything that goes into spec and is in immediate use by the view).
Since the assignment prompts me to make the validity checking function, I'm assuming lines don't own stations or aren't even stored as seperate object because if I'm making a validity check, it makes less sence to enforce immdiate validity throu ownership, hence, this part can be abstracted away.
The name of the project is sub
Since search is O(1), and because stations already aren't connected, owning lines don't make any sense, hence a Line object is an allowed just line name for stations to have. having owning lines as is suggested by the assignment would also make search and validation a lot more complicated, particularly for MultiStations and TransitNodeStations, and would also be less space efficient and less performant with no benefits for robustness nor for extendability of the Model.
